---
###############################################################################
# Cache (core.conf)
###############################################################################

# Cache size (16 KiB blocks). Default: 512.
deluge_cfg_core_cache_size: 512

# Cache Expiry (seconds). Default: 60 (1 minute).
deluge_cfg_core_cache_expiry: 60

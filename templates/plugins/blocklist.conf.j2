{
    "file": 1,
    "format": 1
}{
    "check_after_days": {{ _deluge_cfg_plugins_blocklist.raw.check_after_days }},
    "last_update": {{ _deluge_cfg_plugins_blocklist.raw.last_update }},
    "list_compression": "{{ _deluge_cfg_plugins_blocklist.raw.list_compression }}",
    "list_size": {{ _deluge_cfg_plugins_blocklist.raw.list_size }},
    "list_type": "{{ _deluge_cfg_plugins_blocklist.raw.list_type }}",
    "load_on_start": {{ _deluge_cfg_plugins_blocklist.raw.load_on_start | string | lower }},
    "timeout": {{ _deluge_cfg_plugins_blocklist.raw.timeout }},
    "try_times": {{ _deluge_cfg_plugins_blocklist.raw.try_times }},
    "url": "{{ _deluge_cfg_plugins_blocklist.raw.url }}",
{% if _deluge_cfg_plugins_blocklist.raw.whitelisted | length > 0 %}
    "whitelisted": [
{%   for whitelist in _deluge_cfg_plugins_blocklist.raw.whitelisted %}
        "{{ whitelist }}"{% if not loop.last %},{% endif %}

{%   endfor %}
    ]
{% else %}
    "whitelisted": []
{% endif %}
}